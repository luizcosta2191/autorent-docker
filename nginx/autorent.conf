server {
    listen 80;
    listen [::]:80;

    server_name autorent.192.168.56.10.nip.io autorent.local localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
